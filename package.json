{
  "name": "json-server",
  "version": "1.0",
  "description": "REST API for testing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.16.3"
  }
}
